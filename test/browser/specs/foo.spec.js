import { ChildNodeSpan } from '../dist/src/browserUtils.js'

const assert = chai.assert

describe('ChildNodeSpan', () => {
    describe('with zero children', () => {
        it('returns 0', () => {
            assert.equal(0, 1)
        })
    })

    describe('with zero children', () => {
        it('returns 0', () => {
        })
    })
})
