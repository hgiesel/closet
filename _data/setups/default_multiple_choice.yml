name: Default Multiple Choice
code: |
  const recipes = [
    Closet.recipes.multipleChoiceShow({
      tagname: 'mc',
    }),
    Closet.recipes.multipleChoiceHide({
      tagname: 'mch',
    }),
    Closet.recipes.multipleChoiceReveal({
      tagname: 'mch',
    }),
  ]

  for (const recipe of recipes) {
    filterManager.addRecipe(recipe)
  }

