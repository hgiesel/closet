name: Occlusions with highlighting border
code: |
  const backProperties = {
    fill: 'transparent',
    strokeWidth: '2px',
    stroke: 'red',
  }

  filterManager.install(
    closet.browser.recipes.rect({ tagname: 'rect', backProperties }),
  )
