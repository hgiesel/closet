name: "Shuffle and Order"
code: |
  filterManager.addRecipe(Closet.recipes.shuffling({ tagname: 'mix' }))
  filterManager.addRecipe(Closet.recipes.ordering({
    tagname: 'ord', 
    shuffleTagname: 'mix',
  }))
